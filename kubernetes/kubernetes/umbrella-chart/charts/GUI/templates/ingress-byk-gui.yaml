{{- if .Values.gui.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.gui.release_name }}-ingress
  labels:
    app: {{ .Values.gui.release_name }}-ingress
  {{- with .Values.gui.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.global.gui.ingress.tls }}
  tls:
    {{- range .Values.global.gui.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.gui.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- if .pathType }}
            pathType: {{ .pathType }}
            {{- end }}
            backend:
              service:
                name: {{ $.Values.gui.release_name }}  # Use $ to refer to root context
                port:
                  number: {{ $.Values.gui.service.port }}
          {{- end }}
    {{- end }}
{{- end }}