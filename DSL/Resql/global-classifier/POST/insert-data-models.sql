INSERT INTO public.data_models (
    model_group_key,
    model_name,
    major,
    minor,
    latest,
    deployment_env,
    training_status,
    base_models,
    last_trained,
    connected_ds_id,
    connected_ds_major_version,
    connected_ds_minor_version,
    model_status,
    model_s3_location,
    training_results,
    created_timestamp,
    updated_timestamp
) VALUES (
    (FLOOR(RANDOM() * 999999)::TEXT || '_' || EXTRACT(EPOCH FROM NOW())::BIGINT::TEXT),
    :modelName,
    1,
    0,
    true,
    :deploymentEnv::deployment_environment,
    'initiating_training'::training_status,
    :baseModels::JSONB,
    NULL,
    :connectedDsId,
    :connectedDsMajorVersion,
    :connectedDsMinorVersion,
    'active'::model_status,
    NULL,
    NULL,
    CURRENT_TIMESTAMP,
    CURRENT_TIMESTAMP
)
RETURNING 
    model_id,
    model_group_key,
    model_name,
    major,
    minor,
    latest,
    deployment_env,
    training_status,
    base_models,
    created_timestamp,
    connected_ds_id,
    connected_ds_major_version,
    connected_ds_minor_version;