declaration:
  call: declare
  version: 0.1
  description: "Primary text classification endpoint for Buerokratt chatbot services"
  method: post
  accepts: json
  returns: json
  namespace: global-classifier
  allowlist:
    body:
      - field: chatId
        type: string
        description: "Unique identifier for the chat session"
      - field: message
        type: string
        description: "Text message to be classified"
      - field: url
        type: string
        description: "URL context for the chat session"

extractClassificationData:
  assign:
    chatId: ${incoming.body.chatId}
    message: ${incoming.body.message}
    url: ${incoming.body.url ?? ""}
    startTime: ${Date.now()}
  next: validate_input

validate_input:
  switch:
    - condition: ${!chatId}
      next: return_validation_error
    - condition: ${!message}
      next: return_validation_error
  next: get_production_model

get_production_model:
  call: http.post
  args:
    url: "[#GLOBAL_CLASSIFIER_RESQL]/get-production-model-id"
    body: {}
  result: model_result
  next: check_model_availability
  error: return_model_error

check_model_availability:
  switch:
    - condition: ${!model_result.response.body || model_result.response.body.length == 0}
      next: return_no_model_error
  assign:
    modelId: ${model_result.response.body[0].modelId}
  next: prepare_inference_payload

prepare_inference_payload:
  assign:
    ensembleModelName: ${modelId}-classifier-ensemble
  next: call_triton_inference

call_triton_inference:
  call: http.post
  args:
    url: "[#GLOBAL_CLASSIFIER_PRODUCTION_ENV_MODEL_SERVER]/v2/models/${ensembleModelName}/infer"
    body:
      inputs:
        - name: "TEXT"
          datatype: "BYTES"
          shape: [1, 1]
          data:
            - ${message}
    headers:
      Content-Type: "application/json"
  result: inference_result
  next: process_classification_result
  error: return_inference_error

process_classification_result:
  assign:
    # Parse the JSON string output from Triton to get agency classification array
    rawOutput: ${inference_result.response.body.outputs[0].data[0]}
    parsedOutput: ${JSON.parse(rawOutput)}
  next: log_inference_result

log_inference_result:
  call: http.post
  args:
    url: "[#GLOBAL_CLASSIFIER_RESQL]/insert-inference-log"
    body:
      chatId: ${chatId}
      url: ${url}
      modelId: ${modelId}
      inferenceTimeMs: ${Date.now() - startTime}
      parsedOutput: ${JSON.stringify(parsedOutput)}
  result: log_result
  next: return_classification_success
  error: return_log_inference_error

return_classification_success:
  status: 200
  return: ${parsedOutput}
  next: end


return_log_inference_error:
  status: 500
  return: "Error logging inference result in PostgresDB"
  next: end


return_validation_error:
  status: 400
  return: "Invalid payload. chatId and message are required."
  next: end

return_no_model_error:
  status: 503
  return: "No production model available for classification"
  next: end

return_model_error:
  status: 500
  return: "Failed to retrieve production model configuration"
  next: end

return_inference_error:
  status: 500
  return: "Classification service temporarily unavailable"
  next: end